<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Tasks (phase 4): the task content type and template, checklists, subtasks and dependencies,
    recurring tasks, cross-list "my tasks" views, tasks from documents.
    Built on the public extension SDK only (EXT-06; enforced by the architecture tests).
  -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <ProjectReference Include="..\..\..\Extensions\PaperDotNet.Extensions.Abstractions\PaperDotNet.Extensions.Abstractions.csproj" />
    <ProjectReference Include="..\..\..\Extensions\PaperDotNet.Extensions.Analyzers\PaperDotNet.Extensions.Analyzers.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
  </ItemGroup>
  <ItemGroup>
    <!-- RRULE parsing and expansion (RFC 5545). -->
    <PackageReference Include="Ical.Net" />
  </ItemGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="PaperDotNet.UnitTests" />
    <InternalsVisibleTo Include="PaperDotNet.IntegrationTests" />
  </ItemGroup>
</Project>
